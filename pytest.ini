[pytest]
# Pytest configuration for Gym Tracker Bot

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = 
    -v
    --tb=short
    --strict-markers
    --color=yes

# Test markers
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (slower, multiple components)
    slow: Slow tests that take more than a few seconds
    database: Tests that require database setup
    external: Tests that require external services
    
# Minimum version
minversion = 6.0

# Coverage options (if pytest-cov is installed)
# Uncomment the following line to enable coverage locally:
# addopts = --cov=src --cov-report=html --cov-report=term-missing --cov-report=xml

# Async test support
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Filtering
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning:telegram.*
    ignore::pytest.PytestUnhandledThreadExceptionWarning
    ignore::pytest.PytestUnknownMarkWarning